package model;

public class SystemManager extends User{
	
	public SystemManager(String account, String password, String name, String contactInfo, String type) {
		super(account, password, name, contactInfo, type);
	}

}
